const names = require('./app');

for (let i = 0; i < Object.values(names).length; i++) {
    console.log(`My name is Alan ${Object.values(names)[i]}`);
}


const comes = require("./app");
console.log(comes);
